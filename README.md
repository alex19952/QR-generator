# QR Generator

QR Generator — настольное приложение на Python для **автоматической генерации этикеток с QR-кодами** на основе Excel-таблиц. Предназначена для проведения инвентаризации на предприятии. Программа загружает данные из файла Excel, позволяет выбрать тип шаблона этикеток (12, 24, 40 или 65 на лист), указать начальную ячейку и сформировать документ Microsoft Word с готовыми наклейками. Дополнительно приложение может собрать CSV- и Excel-базу для терминалов сбора данных (ТСД).

## Возможности
- 📄 Импорт данных из существующих Excel-файлов (`.xlsx`).
- 🏷️ Поддержка четырёх готовых шаблонов этикеток (12 / 24 / 40 / 65 на лист) с предварительным просмотром описания.
- 🎯 Гибкое управление стартовой позицией печати внутри листа.
- 🖨️ Генерация Word-документов (`.docx`) с QR-кодами и описаниями, готовых для печати.
- 📦 Опциональное формирование разбитой по кабинетам базы данных для ТСД (CSV + Excel).
- ⚙️ Возможность настройки параметров шаблонов через модуль `Templates.py`.


![](/docs/images/main.jpg)
![](/docs/images/first_cell.png)

## Архитектура проекта
```
├── Main.py          # Точка входа, передаёт параметры в библиотеку GUI
├── library.py       # Логика генерации документов, работа с Excel и Word
├── GUI.py           # PyQt5-интерфейс, выбор шаблонов и параметров
├── Templates.py     # Набор классов с параметрами листов наклеек
├── Resourses/       # Иконки, изображения и gif-анимация для интерфейса
```

## Требования
- Python 3.8+
- Windows (рекомендуемая ОС: приложение рассчитано на запуск PyInstaller-экзешника и использует Windows Explorer для открытия папок)
- Зависимости Python:
    - `PyQt5`
    - `python-docx`
    - `openpyxl`
    - `qrcode`

## Экспортируемые файлы
- `QR коды.docx` (или имя, указанное при сохранении) — листы с QR-наклейками.
- `base_YYYY-MM-DD_HH-MM/base <номер кабинета>.csv` — CSV-файлы с данными, если включена опция «Создать базу для ТСД».
- `<имя>base YYYY-MM-DD_HH-MM.xlsx` — резервная копия исходного Excel с отметкой времени.

## Сборка в исполняемый файл
Для развёртывания без Python можно собрать проект в `.exe` при помощи [PyInstaller](https://pyinstaller.org/):
```bash
pyinstaller Main.py --hidden-import "library" -w
```
- Папку `Resourses` необходимо положить рядом с исполняемым файлом.
- При упаковке PyInstaller модуль `library.py` должен быть доступен (см. параметр `--hidden-import`).

## Настройка шаблонов
Параметры каждого типа этикеток (размеры страниц, отступы, количество строк/столбцов, размер шрифта и т. д.) находятся в `Templates.py`. Вы можете создать собственный класс шаблона, скопировав существующий и изменив значения по необходимости.
